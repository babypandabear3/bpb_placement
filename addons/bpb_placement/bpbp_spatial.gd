tool
extends Spatial

class_name BPB_Placement_Spatial
export (bool) var paint = true
export (bool) var grid = true
export (float) var grid_size = 1.0
export (int) var grid_layer = 0
export (Array, String, FILE) var library

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
